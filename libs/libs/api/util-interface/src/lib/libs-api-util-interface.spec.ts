import { libsApiUtilInterface } from './libs-api-util-interface';

describe('libsApiUtilInterface', () => {
  it('should work', () => {
    expect(libsApiUtilInterface()).toEqual('libs-api-util-interface');
  });
});
